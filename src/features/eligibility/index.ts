/**
 * Public API of the eligibility slice — the only file other layers may import
 * (docs/architecture/module-boundaries.md).
 */
export { EligibilityQuiz } from "./components/eligibility-quiz";
export { EligibilityQuizProvider } from "./provider";
